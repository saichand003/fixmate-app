/** @type {import('next').NextConfig} */
// const nextConfig = {
//   experimental: {
//     turbo: false //turn off Turbopack
//   }
// };

// module.exports = nextConfig;

// next.config.js
const nextConfig = {
  reactStrictMode: true,
  // other config here, but no "experimental.turbo"
};

module.exports = nextConfig;
